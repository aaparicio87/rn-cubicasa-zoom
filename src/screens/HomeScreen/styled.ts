import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
        padding: 20,
        gap: 10,
    },
    button: {
        marginVertical: 10,
        width: '80%',
        padding: 8,
    },
    titleStyle: {
        color: '#000',
        marginBottom: 10,
    },
    labelButton: {
        color: '#000',
        fontSize: 16,
    },
})